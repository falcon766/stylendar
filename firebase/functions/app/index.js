const invitations = require("./invitations");
const newsfeed = require("./newsfeed");
const notifications = require("./notifications");
const profile = require("./profile");

module.exports = { invitations, newsfeed, notifications, profile };
